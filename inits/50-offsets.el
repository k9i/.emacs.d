;; -*- Emacs-Lisp -*-
;; ~/.emacs.d/init-offsets.el
;;
;; (load "~/.emacs.d/init-offsets")
;; key customizations for Emacs 22 or later
;; C-h to Backspace etc.
;;
(setq sh-basic-offset 2)
(setq sh-indentation 2)
; (setq sh-shell-file "/bin/sh")
